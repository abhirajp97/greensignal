{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "cd /Users/abhirajparikh/src/greensignal && uv run ruff format --quiet \"$CLAUDE_TOOL_INPUT_FILE_PATH\" 2>/dev/null || true",
            "timeout": 10
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "if echo \"$CLAUDE_TOOL_INPUT_FILE_PATH\" | grep -q '\\.env$'; then echo 'BLOCKED: Never write to .env — contains real secrets. Edit .env.example instead, then ask the user to copy values manually.'; exit 2; fi",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
