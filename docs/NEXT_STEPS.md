# GreenSignal — Next Steps

This file is the handoff note between sessions. Claude updates it automatically at the end of each working session. All collaborators should read it at the start of any session and update it when priorities shift.

---

## Current focus

**Phase 1 — Real data pipelines and backtest validation**

The skeleton is built and the architecture is locked. The immediate work is implementing the first three data sources (no GEE needed) and running them through the backtest notebooks to validate signal rank order on real data before any product work begins.

**Multiple composite formulations are under active parallel exploration by different
collaborators — not yet converged, and that's intentional for now:**
- The multiplicative formula in `CLAUDE.md` / `docs/GreenSignal_Math_Reference.md`
  (`multiplier = (1.5 - price_position) × (1.0 + 0.65 × climate_risk_score)`), implemented
  in `domains/coffee/models/signal_generator.py` — this is the one being validated and
  improved in `notebooks/coffee_backtests/06_composite_backtest.ipynb`.
- An additive percentile-score system in
  `docs/greensignal_procurement_intelligence_architecture.md` §11–12
  (`price_percentile_2y`-based point bonuses, thresholded at a score).
- Producer FX as a possible additional input (`08_producer_fx_signal.ipynb`, by
  `aaditshah97`) — tested as a direct timing signal and shelved ("better framed as a
  conditioning/regime variable than a composite timing input"), PR #5 (same collaborator)
  still open.

Do not treat any one of these as "the" final design without checking with the team —
reconciling them is a deliberately deferred decision.

---

## Up next (in order)

- [ ] **Register for Nasdaq Data Link** at `data.nasdaq.com` — get API key, add to `.env` ← **do this before next session starts**
- [x] **Implement `domains/coffee/sources/ice_coffee_c.py`** — fetch `CHRIS/ICE_KC1` daily close, return `list[MarketObservation]`
- [x] **Run `notebooks/coffee_backtests/01_ice_price_signal.ipynb`** — ALL GATES PASS: contemp r=+0.852, cost saving=+10.73% (full-history) / +3.71% walk-forward (12/12 years positive), zone monotone; momentum baseline −10.37% validates contrarian; 104w window +15.01%
- [x] **Implement `domains/coffee/sources/world_bank_commodity.py`** — World Bank Pink Sheet; Arabica + Robusta physical prices (free, no auth); two-step fetch (scrape page → download Excel); 15 tests pass
- [x] **Implement `domains/coffee/sources/noaa_enso.py`** — 26 tests passing; NDJ year-boundary handled; -99.9 sentinel skipped
- [x] **Run `notebooks/coffee_backtests/02_enso_signal.ipynb`** — original 24m-lag gate FAILED because the thesis was inverted. Corrected to **El Niño = supply-risk phase** (verified against `docs/enso_coffee_country_matrix.html` + peer-reviewed sources): El Niño droughts Vietnam/Indonesia robusta, La Niña is beneficial there & in Brazil (frost avoidance) and only hurts Colombia. Redefined gate (r ≥ +0.20 vs **fwd** YoY, ~14m lead) **PASSES**: r=+0.288 @15m (KC 2010–24) / +0.327 @15m (WB 2000–24, p=1.4e-8); event study El Niño months→+36.5% fwd-12m vs La Niña −1.7% (t=5.83). Flipped `enso_risk_score` sign in `noaa_enso.py` (now rises with El Niño); 26 tests updated & passing
- [x] **Implement `domains/coffee/sources/cot.py`** — CFTC disaggregated annual ZIPs; filters to `COFFEE C - ICE`; returns weekly net managed-money positions; `_cot_index` (0-100 rolling) + `cot_contrarian_signal` helpers; 20 tests pass
- [x] **Run `notebooks/coffee_backtests/03_cot_signal.ipynb`** — original contrarian gate FAILS (r=−0.05 @ fwd 12m, need ≥ +0.08); thesis is *inverted* on real data. Also fixed `cot.py`: pre-2013 vintages name the date column `Report_Date_as_MM_DD_YYYY` (was silently dropping 2010–2012); broadened prefix match + regression test
- [x] **Rebuilt notebook 03 around the momentum thesis — now PASSES** (`88af1e9`): `r(COT index, fwd 6m change) = +0.144` (p=0.053, n=180) vs gate ≥+0.08. Horizon sweep confirms 6m as the peak. Signal is real but weak/borderline: p at the edge of conventional significance, 3yr rolling stability only 45% positive. Walk-forward $ savings don't materialize (momentum signals don't reduce weighted-avg purchase price the way contrarian ones do) — notebook concludes the correct role is a **low-weight composite amplifier, not a standalone timer**. `cot_contrarian_signal()` in `cot.py` still encodes the old disproven contrarian sign — needs updating to the momentum framing when wired into `signal_generator` (notebook 06)
- [x] **Implement `domains/coffee/sources/usda_psd.py`** (L2a) — bulk ZIP → world stocks-to-use %; corrected stub assumptions (commodity code is int `711100`; no World row → sum 94 countries; attr `125`=Domestic Consumption not `57`); `stu_risk_score` + `load_from_csv` helpers; 20 tests pass. Real data: world S/U 22% (2018) → 11.6% (2025)
- [x] **Run `notebooks/coffee_backtests/04_usda_supply_signal.ipynb`** — YoY-change gate FAILS (r=−0.04) but signal is **strong on price level**: r=−0.40 monthly, −0.56 annual (n=15), −0.59 @ 23m lag. S/U is a slow annual stock var — YoY-change is the wrong lens
- [x] **Rebuild notebook 04 for look-ahead bias + redefined dual gate** — the PSD bulk file is the latest-*revised* vintage of every marketing year, so the original r=−0.40 to −0.59 was contaminated by information a roaster wouldn't have had in real time. Fixed with a practical 12m-forward shift of the monthly series (simulates publication lag); raw r weakens to −0.26 once corrected. Redefined gate to two tests, **both PASS**: Gate 1 r(vintage S/U, 12m-fwd price)=−0.312 (p=2.0e-5); Gate 2 r(S/U YoY delta, price level)=−0.259 (p=4.6e-4). Added 10yr rolling z-score (no look-ahead), YoY delta, months-of-consumption features, and a z-score-based non-linear stress score replacing the linear 12%/35% clamp. Rolling 3yr stability is honest but modest (52% of windows negative, up from 23% on the old YoY basis) — full-sample gates are the stronger evidence
- [x] **Implement `domains/coffee/sources/usda_coffee_wmt.py`** — true vintage-aware world stocks-to-use %, superseding the deferred "build a WASDE-vintage series" item (that scoping was based on an incorrect premise: **WASDE does not cover coffee at all** — the correct USDA report is the separate semiannual "Coffee: World Markets and Trade" circular, archived to June 2004 at `esmis.nal.usda.gov`). Discovers the archive by paginating the ESMIS listing (had to fix a real bug: the pager loops back to page-0 content past the real archive depth instead of returning empty — pagination now stops on "no new dates added," not "empty page"), downloads + parses each circular's summary table (format drifted across 20 years; anchored on the literal phrase "Thousand 60-Kilogram Bags" to avoid a false-positive page-1 narrative subsection also titled "Ending Stocks"). 31 reports parse cleanly (2010-06 to 2025-06); pre-June-2010 issues fail to parse and are skipped (acceptable — outside the notebook's core window). Cross-validated: Dec 2025 parse (11.59%) matches `usda_psd.py`'s independent 11.6% for the same period. 24 tests pass (all HTTP/PDF calls mocked). Reviewed via `data-source-reviewer` — **APPROVED**. New main deps: `pdfplumber`, `beautifulsoup4`
- [x] **Wire the true vintage source into notebook 04** — new §11 fetches `usda_coffee_wmt.fetch()`, aligns the irregular semiannual dates to price via `pd.merge_asof`, and re-runs both gates: Gate 1 r=−0.488 (p=6.2e-3, n=30) — *stronger* than the 12m-shift approximation's −0.312; Gate 2 r=−0.261 (p=0.17, passes the threshold but not significant at this small semiannual sample). Approximation and true series agree directionally but only moderately (r=+0.49 between them, mean abs diff 3.1pp) — kept the approximation as a documented fallback (monthly cadence, covers pre-June-2010)
- [x] **Rebuilt `stu_stress` off the true-vintage series (notebook 04 §16) and re-ran the L2a ablation (notebook 06 §11-§12) — a real negative finding, not the hoped-for confirmation.** window=6 small-n z-score (n=25 valid semiannual observations, 2013-06 to 2025-06). True-vintage `stu_stress` is **wrong-signed** against `fwd_6m` (r=−0.135 vs the approximation's +0.262) and dropping it from the composite now *improves* walk-forward cost improvement by +0.79pp — worse than the approximation's near-neutral −0.03pp. L2a still passes its own standalone gate against 12m-forward price *level* (r=−0.488) — a different target than the composite's 6m-forward-*return* ablation, not a contradiction. **Weights re-derived and promoted into `climate_features.py::WEIGHTS`**: stu_risk 0.201 (down from Phase 0's 0.38), enso_risk 0.142, brazil_drought_risk 0.625 (now dominant), cot_contrarian 0.031. Walk-forward with final weights: +4.45% (2017-2024, PASS vs 3.0% gate). User confirmed the down-weighting approach (r-proportional, following the ablation evidence) after reviewing the finding
- [ ] **Promote `stu_z_score` / `stu_stress_score` / the true-vintage series** from notebook 04 into `domains/coffee/features/supply_features.py` (currently a stub) — mirrors the SPI-promotion item below for L3. Still pending; the weight promotion above only touched `climate_features.py::WEIGHTS`, not the underlying risk-score computation functions
- [ ] *(optional, low priority)* Extend `usda_coffee_wmt.py` coverage before June 2010 — older circulars use a different/unparseable format; only worth it if the composite (06) shows L2a's weight is sensitive to the extra 6 years of history
- [ ] *(optional, low priority)* Wire `usda_coffee_wmt.fetch()` into a scheduled job (`jobs/coffee/monthly_supply.py`, currently a stub) to build a running SourceRun vintage history in production — a Phase 1 scheduling task, not a backtest task
- [x] **Implement `domains/coffee/sources/chirps.py`** (L3) — GEE CHIRPS PENTAD over Minas Gerais GAUL polygon; monthly area-mean rainfall + `drought_risk_score`; NetCDF fallback; 20 tests. GEE project `western-plate-432020-t5`, auth via `EARTHENGINE_PROJECT` env
- [x] **Run `notebooks/coffee_backtests/05_chirps_signal.ipynb`** — ~~gate narrowly FAILS (r=+0.10 monthly @ 14m lag)~~ **REBUILT around SPI (desk review).** Primary = flowering **SPI-3 (Sep–Nov) deficit** vs fwd-12m price on the **annual crop-year frame**: r=**+0.483 (p=0.069, n=15)** → **PASSES** redefined gate (annual r ≥ +0.30, confirming signal). Deficit `max(0,−SPI3)` beats signed SPI (−0.412 → asymmetric); robust to look-ahead (expanding r=+0.494) & to a stocks-to-use control (partial r=+0.48); driest vs wettest flowering third → +33.6% vs +9.5% fwd-12m. Old monthly r≥+0.12 lens diluted an annual once-a-year signal
- [ ] **Promote the SPI flowering-deficit feature** from notebook 05 into `domains/coffee/features/climate_features.py` (canonical L3 feature; replaces the provisional mm-anomaly `drought_risk_score` in `chirps.py`). Feed the composite as a continuous z-score, not a 0/1 flag
- [ ] *(deferred, later phase)* Climate sub-score (L3 + ENSO + **Vietnam monsoon** robusta source) → composite/06; new-crop (U/Z) contract target → needs paid ICE data; forward-cover ratio / OTM-call timing / risk limits → product layer (Phase 5–6). All logged in notebook 05 §10
- [x] **Run `notebooks/coffee_backtests/06_composite_backtest.ipynb`** (first version) — all 5 signals aligned onto one monthly frame. Gate PASSED on a discrete "forward prescience after BUY" metric, but on a thin sample (11–21 buy-months/10yrs, 3–4 zero-buy years) — flagged by the product owner as a real problem: a tool going silent for years can't serve a weekly purchasing decision
- [x] **Rebuilt notebook 06 around a continuous signal, matching the product's actual design** — re-read `docs/GreenSignal_ICP.md`/`coffee_intelligence_mvp.md`/`GreenSignal_Phase0_Report.md`/`GreenSignal_Math_Reference.md`: GreenSignal is designed as an always-on 3-state (BUY/NEUTRAL/CAUTION) signal, not a rare alert (Phase 0 target: L1 alone fires BUY 22% of months; real data: 35–41%, not sparse). Root cause found and fixed: the old walk-forward re-derived its normalization baseline once per *year* (annual-refit) instead of continuously — a **rolling 24-month** trailing normalization raised the BUY rate from 4.9% to 20.0% and cut the longest silent gap from 18 to 16 months. Switched the **primary gate** to the continuous `cost_improvement_backtest` methodology (Math Reference §11.1, same one notebook 01 used for L1) instead of the discrete prescience test (now secondary) — **PASSES: walk-forward +5.86%, beating L1-alone's own +3.71% benchmark**. Confirmed the spike-avoidance story directly: composite held sustained BUY Jan–Sep 2023, before the 2024 rally accelerated past $220/lb
- [x] **Two honest open anomalies flagged in the rebuilt notebook, not resolved** — (1) the secondary prescience check shows BUY months *underperforming* CAUTION/NEUTRAL forward returns in this sample (uses in-sample fixed weights, not the walk-forward weights that pass the primary gate; plausibly reflects the 2023–2025 structural supply-shock rally where mean-reversion timing underperforms momentum) — needs further investigation before trusting either direction; (2) the leave-one-out ablation reverses sign for L5 (`cot_momentum`) vs. the original notebook's ablation (now costs +0.56pp to drop it, was −0.81pp) — which sub-signals earn their weight is sensitive to the validation metric used, a reason to treat any single ablation as provisional
- [ ] **Reconcile the §7/§8 anomalies above, and rebuild `stu_stress` off the true-vintage series (n=30 semiannual)** before finalizing weights — the current PSD-approximation-based `stu_stress` may be actively hurting the composite (near-neutral to slightly negative contribution across both notebook versions' ablations); needs its own small-n windowing, then re-run the ablation for L2a specifically
- [ ] **Production wiring (after the items above)** — fill in `core/services/recommendation_engine.py::build_recommendation` (currently a stub — this is where the multiplier formula belongs, including the rolling-24m normalization), update `domains/coffee/features/climate_features.py::climate_risk_score`'s hardcoded Phase 0 weights with the notebook-06-validated ones, implement `domains/coffee/models/risk_scorer.py` (stub), and promote `stu_risk_score`/`enso_lagged`/the SPI drought score into their production feature files (all deferred until this point, per the L3/SPI precedent). Note: per the multi-collaborator exploration state (top of this file), confirm which composite formula is being wired before doing this — this notebook validates the multiplicative formula specifically, not necessarily the converged team design
- [ ] **Then: independent engineering work** — `core/storage/repositories.py` (stub — none of the 6 working sources currently persist anything, `records_stored` is always 0) + wiring `jobs/coffee/*.py` (all 3 stubs), `domains/coffee/features/margin_features.py` (stub, pure arithmetic). Sequenced after the composite/production-formula work per this session's agreed plan — genuinely independent of the composite weights, could also be parallelized if priorities shift

### India origin signal (parallel workstream — separate from the notebook 06 anomaly reconciliation above, does not block it)

**First pass (2026-07-21)** settled for a World Bank global-benchmark price proxy
after concluding no India-origin price source was reachable — see below, this
conclusion was wrong and has been corrected.

- [x] Corrected `docs/india_origin_signal_plan_v2_full_build.md` against current repo state (v2.1) — the outdated v2 doc had several wrong assumptions (FRED source that doesn't exist, invented `RegionSpec` class, wrong `FeatureObservation` field name, unnecessary new API routes, GAUL level-1 vs level-2 confusion)
- [x] Task 0 price/climate audit, first pass — timeboxed ~30-45min, concluded no India-origin price source existed (only tried `indiacoffee.org`, which is dead) and fell back to the WB benchmark as a proxy
- [x] Found and fixed a live bug in `world_bank_commodity.py` while running Task 0 — World Bank dropped the machine-code header row from the Pink Sheet Excel in a 2026-07-02 update, silently breaking the live fetch. Fixed regardless of India, since it was a real production bug
- [x] `chirps_india.py`, `build_recommendation()`, `generate_india_signal()` implemented — all still real and in use in the corrected pass below

**Second pass (2026-07-22) — corrected the price-source conclusion and rebuilt on real data.** Told explicitly not to settle for a proxy and not to self-impose artificial timelines:

- [x] **Found the real Coffee Board of India domain: `coffeeboard.gov.in`** (not `indiacoffee.org`) — reachable, current, with a 15-year Daily Market Report archive and a semiannual production-estimate PDF circular back to 2009
- [x] **Implemented `domains/coffee/sources/coffee_board_india_price.py`** — scrapes the Daily Market Report archive (a stateful ASP.NET WebForms flow, reverse-engineered by replaying it with `requests.Session()`), extracts genuine India-origin "Raw Coffee Price (Karnataka)" (Arabica/Robusta, Parchment/Cherry, ₹/50kg). Discovered the underlying figure only updates ~weekly despite "daily" bulletins (confirmed by observing repeated values across consecutive days) — each observation dated to the table's own "as on" date so this collapses correctly. Paced (2s/request) and resumable. 16 tests pass. Full backfill: 9,217 observations, dense from 2014
- [x] **Implemented `domains/coffee/sources/coffee_board_india_supply.py`** — parses the semiannual "Database on Coffee" PDF circular for district + national production estimates, vintage-dated per report like `usda_coffee_wmt.py`. 19 tests pass. Full backfill: 1,289 observations, 2010-2024 (PARTIAL — one report mislinked on the government site itself, a real typo)
- [x] **Corrected `chirps_india.py` to species-specific flowering windows** — the first pass used one Feb-Mar window for both species; verified against real agronomy sources that Robusta (Feb-Mar) and Arabica (Apr-May) blossom at different times. `is_flowering_month(month, species)` now requires species explicitly
- [x] **Rebuilt notebook 09 entirely on real data** (price + supply + climate, no proxy) — added a lag sweep (3-24m horizons), a standalone supply-signal test, and a `price_position_52w` sanity check before concluding. **Gate still FAILS for both species**: Robusta r=−0.357 at 12m (n=12, wrong-signed at every horizon tested); Arabica r=−0.038 at 12m (n=12, turns +0.179 at 24m — right sign, still short of +0.30). Supply YoY vs fwd-6m price: r=−0.110, not significant. The sanity check passes cleanly for both species, confirming the price data and `price_position_52w` mechanics are sound — it's specifically the climate/supply-to-price hypothesis that doesn't clear its gate on real data
- [x] Found and fixed a third live-only bug: `httpx.Client` doesn't follow redirects by default (unlike `requests`) — needed `follow_redirects=True` explicitly for the new price source
- [x] Updated `INDIA_ARABICA`/`INDIA_ROBUSTA` registry metadata to point at the real source; added `INDIA_PRODUCTION` asset; unit corrected from a placeholder `"lb"` to the real source's native `"inr_per_50kg"`
- [x] **Production-weighted multi-district climate signal — tried, does not rescue the gate.** Extended `chirps_india.py` with a `district` param (Kodagu/Chikmagalur/Hassan), weighted by production share (52%/33%/14%). Robusta improves marginally but stays wrong-signed (r=−0.357 → −0.285, FAIL); Arabica moves toward zero (r=−0.038 → −0.016, FAIL). Weak evidence against "wrong district" as the cause — the climate mechanism itself looks like the bigger issue
- [x] **Arabica 24m-horizon hint robustness check — tried, hint does not survive scrutiny.** Leave-one-out on r=+0.179 @ 24m ranges [−0.065, +0.395] (sign flips depending on excluded year); 5,000-resample bootstrap 90% CI is [−0.356, +0.622] (spans zero and the gate). Conclusion: the 24m hint was noise, not a real weak effect — only new years of price history can move this, not more analysis of the same n=12
- [x] **Found and fixed a district-name spelling-drift bug** in `coffee_board_india_supply.py` — "Chikkamagaluru"/"Chikmagalur", "Wyanad"/"Wayanad", "Orissa"/"Odisha" (genuine 2011 rename) were splitting one district's series in two. Fixed with a `_REGION_ALIASES` map in `_region_slug()`; deliberately did not merge "Andhra Pradesh" with "Andhra Pradesh & Orissa" (structural difference, not a spelling one). 4 new tests
- [ ] **Blocked on an external dependency**: `coffeeboard.gov.in`'s TLS certificate expired 2026-07-22 (confirmed via direct `openssl` inspection) and the site became unreachable over both HTTPS and HTTP mid-session. A re-fetch attempt combined with a caching-script mistake (writing an empty result over the good CSV without checking success first) destroyed the original 1,289-observation/62-report supply backfill. Recovered a reduced 168-observation/4-edition snapshot from locally-cached PDFs (sufficient for the district-weight experiment above, not for the full national YoY test's original power). **Bypassing SSL verification to force the fetch through was explicitly rejected** as a workaround (MITM/data-integrity risk) — the full 62-report re-backfill must wait for Coffee Board to renew their certificate
- [x] **Whether India price is dominated by global pass-through/currency instead of local climate — tested, confirmed decisively.** External review diagnosed the structural reason (India is ~3.5% of world production, a price taker not a price setter) and it was tested directly in notebook 09 §6-7: `log(india_price) ~ log(global_price) + log(usd_inr)` (World Bank benchmark reused from notebook 07's cache, USD/INR via `yfinance` per notebook 08's pattern) gives **R²=0.887 (Robusta) / 0.962 (Arabica)**, both FX coefficients positive as expected and highly significant. Global pass-through + FX explains 89-96% of India's domestic price. Re-testing the flowering-deficit gate against the *residual* (not raw price) still FAILS both species: Arabica r=+0.033 (zero); **Robusta r=−0.587 (p=0.045) — more significant AND more wrong-signed than the raw-price test**, stable across a 6-24m lag sweep — a genuinely surprising result (wetter flowering → higher later price), reported as such
- [ ] **Product-narrative implication from the pass-through finding, not yet acted on**: a validated India signal is far more accurately framed as global-price-plus-FX than a local-climate timing signal — materially different from `generate_india_signal()`'s current 2-input composite. Feed this into the card-copy review below
- [ ] **Genuinely untested even after the pass-through pass, deliberately deferred as separate, larger-scope work**: (a) discrete regulatory/policy shocks — the EU Deforestation Regulation (EUDR) compliance deadline and the India-EFTA duty changes — as event-study step dummies rather than continuous features; (b) harvest-window (Nov-Feb) rainfall as a structurally different climate pathway (berry drop / fungal disease during an already-picked-or-picking crop) from the flowering-window SPI-3 already built. Domestic consumption growth (84k→91k tonnes, 2012-2023) is slow structural context for product copy, not a backtest feature — not worth building
- [ ] Card copy for the India signal has not been through the full calibrated-honesty review yet — do this before any roaster-facing demo, not just an internal one. The message is now a different shape than "proxy, pending real data" or even "climate mechanism tested and failed": real India-origin data throughout, mechanically sound, and the dominant driver of India price is now known (global pass-through + FX) — the product framing should reflect that, not the original local-climate-signal concept
- [ ] **Blocked on the global composite's production wiring (Build Sequence step 5, `build_recommendation`'s weights): validate the global composite's *forecasting* transferability to India, not just the §14 contemporaneous fit.** §14 proved India price and (global price + FX) move together at the same time (R²=0.887/0.962) — it did NOT prove that the global composite's forward-looking recommendation, translated through that pass-through equation, correctly anticipates *forward* India price moves. Once the global composite produces real historical multipliers/recommendations: translate through `india_price_predicted = global_price_predicted^0.937 × fx^0.947` (Arabica's own elasticities — **scope to Arabica first**, its 3.8% unexplained residual is less than a third of Robusta's 11.3%, and Robusta's larger residual is exactly where the wrong-signed climate result showed up) and correlate against forward India price changes, gated the same way as the original composite. A FAIL here would be a genuinely useful finding too — it would mean India price *levels* track global+FX but India price *timing* moves differently (adjustment lags, sticky domestic pricing). Full writeup and rationale in `docs/india_origin_signal_plan_v2_full_build.md` §15.
- [ ] **Two questions this same feedback surfaced that need an explicit answer before/alongside the test above, not an implicit default:** (1) FX (USD/INR) has an elasticity (0.947) nearly matching coffee's own — a real, currency-driven (RBI policy, capital flows, US rates) component of any future India price move that the product needs an honest view on, not something silently folded into "the coffee story." (2) Who is the India roaster customer — an India-based roaster paying in rupees (FX is directly actionable for them) or a US/EU roaster buying India-origin lots via a USD-pricing importer (FX mostly washes out for them; what matters is the raw global signal + India-specific quality/availability risk from the climate work)? These may be two different products under one "India signal" label.
- [ ] **Larger architectural note worth keeping in view, not a near-term task:** if the transferability test above holds, it implies the product doesn't need N independently-backtested composites per origin (other coffee origins, or cacao per the expansion path) — just one validated global composite plus a lightweight per-origin translation layer (pass-through elasticity + explicit FX view + known event risks like EUDR flagged separately). Worth remembering when cacao's own origin work starts.

### Tooling / infra
- [x] **Add CI** — `.github/workflows/verify.yml` runs `make verify` (check-imports + lint + test) on push to `main` + PRs. Closes the gap that let a lint-breaking commit land (verify-before-commit was CLAUDE.md convention only, never enforced)
- [ ] **Enable branch protection on `main`** (GitHub repo admin) — require the `verify` status check to pass before merge/push. Until this is set, CI is informational only and a red run does not block. Both founders should also confirm their Claude Code loads the project `.claude/settings.json` hooks + `CLAUDE.md` (they are checked in, not gitignored)
- [ ] *(optional)* Add a CHANGELOG-freshness CI nudge (warn if `domains/`, `core/`, or notebooks changed without a `CHANGELOG.md` edit) — deterministic reminder for the doc-update convention. Hold unless doc drift recurs

---

## Blocked / waiting

| Item | Blocked on |
|------|-----------|
| ~~`chirps.py` implementation~~ | ✅ done — GEE access granted, project `western-plate-432020-t5` |
| Supabase storage wiring | Not needed until signals are validated |
| FastAPI routes (coffee) | Not needed until signals are validated |

---

## Decisions made this session

| Decision | Rationale |
|----------|-----------|
| Canonical models use Pydantic `BaseModel` | FastAPI serialization, no extra layer needed |
| `forecaster.py` deleted | Pointless wrapper until `fit_and_forecast` is real |
| `make verify` is the pre-commit gate | Import boundary + lint + tests in one command |
| Repo is public on GitHub | Required for branch protection on free plan; no secrets in code |
| `@anshquant` added as Admin collaborator | Anshumaan Gandhi — co-developer |
| `.claude/settings.json` added to repo | Ruff auto-format on every Edit/Write; `.env` write blocked at hook level |
| `.mcp.json` added to repo | GitHub + Supabase MCP servers wired; auth via env vars, not secrets in config |
| `implement-source` skill created | Enforces consistent source contract — prevents drift across 5 implementations |
| `backtest-notebook` skill created | Enforces consistent notebook structure — all 6 notebooks follow same template |
| `data-source-reviewer` agent created | Independent review gate before any source is merged |

---

## Backtest pass/fail gates (do not start product work until all pass)

See `notebooks/coffee_backtests/README.md` for the full table. Summary:

| Signal | Minimum r on real data |
|--------|----------------------|
| L1 price position | ≥ +0.50 |
| L2a stocks-to-use | ≤ −0.25 |
| L2b ENSO ~14m lead (El Niño) | ≥ +0.20 vs fwd YoY (redefined) |
| L3 CHIRPS drought (SPI flowering deficit, annual) | ≥ +0.30 (redefined) |
| L5 COT momentum (redefined from contrarian) | ≥ +0.08 @ fwd 6m |
| Full composite, cost-improvement (redefined primary gate, walk-forward) | ≥ 3.00% vs naive |

---

---

## Decisions made this session

| Decision | Rationale |
|----------|-----------|
| `fetch()` returns `tuple[list[MarketObservation], SourceRun]` | Skill contract — sources log their own run metadata; storage is the job's responsibility |
| Use `Settle` column (not `Last`) for ICE KC price | Settlement price is the official exchange close; `Last` is the final trade and can differ |
| `raw` field stores `dict(zip(column_names, row))` | More useful than a bare list; survives column reordering |
| `datetime.now(UTC)` instead of `utcnow()` | `utcnow()` deprecated in Python 3.12+ |
| `data_quality.py` stubs implemented | Required for `ice_coffee_c.py` to call gap/range checks |
| `core/models/__init__.py` bug fixed | Was importing `Scenario` (doesn't exist); corrected to `ScenarioInput`, `ScenarioOutput` |
| Nasdaq Data Link CHRIS requires paid plan | API returns 403 for CHRIS database; backtest uses Yahoo Finance `KC=F` (equivalent instrument) |
| L1 gate redefined: 3 tests instead of 1 | Phase 0 r=+0.64 was contemporaneous (trivially high); corrected to contemp r + cost-saving + zone monotonicity |
| Arabica mean-reversion is 24m, not 12m | Forward predictive r peaks at 24m lag (r=+0.20); at 12m r≈0 due to momentum-then-revert pattern |
| Real cost-saving is 10.73%, not 2.2% | Phase 0 synthetic underestimated because real prices had more extreme episodes (2024 spike etc.) |

| Walk-forward cost saving is +3.71% (not +10.73%) | Full-history number is in-sample; walk-fwd is the honest out-of-sample estimate — cite this in product/investor comms |
| Momentum baseline is −10.37% vs naive | Trend-following hurts for coffee procurement; contrarian approach validated decisively |
| 104w window (+15.01%) outperforms 52w (+12.95%) | Noted for future composite tuning; not changing current L1 signal until all 5 signals validated |
| L2a z-score/stress-score features stay notebook-local for now | Mirrors the L3/SPI precedent (notebook 05) — validated features are promoted into `domains/coffee/features/*.py` when the composite (06) actually wires them in, not immediately after a backtest |
| WASDE does not cover coffee — corrected mid-session | Review feedback's suggestion to parse "WASDE" reports for a vintage-aware S/U series was based on an incorrect premise (verified via web search: WASDE is grains/oilseeds/cotton/sugar/livestock only). The correct report is USDA FAS's separate semiannual "Coffee: World Markets and Trade" circular — built as `usda_coffee_wmt.py` instead |
| `usda_coffee_wmt.py` pagination stops on "no new dates," not "empty page" | The ESMIS archive listing does not return an empty page past the real archive depth — it wraps back to repeating page-0 content. A naive "stop on empty page" check would loop `_MAX_LISTING_PAGES` times re-fetching the same page; caught via a full historical smoke test before writing unit tests |
| True vintage series kept alongside the shift approximation, not replacing it | The true series only covers June 2010 onward (semiannual); the approximation still fills gaps (pre-2010, or monthly cadence between reports) — both are in notebook 04, true vintage as primary evidence |
| L5's momentum role was already settled by notebook 03, not re-litigated | User asked to verify rather than assume — notebook 03 (rebuilt in an earlier session, `88af1e9`) already concludes COT should be a low-weight composite amplifier, not a standalone timer. CLAUDE.md/README/FILE_MAP/NEXT_STEPS were stale (still said FAILS) — fixed as a standalone doc-drift correction before starting notebook 06 |
| Composite weights: r-proportional reweighting (own-frame univariate r), not a joint regression | 4 free parameters fit jointly on borderline signals (L3 p=0.069 n=15, L5 p=0.053) risks fitting noise; r-proportional reweighting is simpler, stays interpretable for product copy, and a Ridge ceiling check didn't beat it — confirms the simpler scheme isn't leaving real performance on the table |
| Notebook 06 ablation run full-history (in-sample) only, walk-forward reserved for the final chosen config | True walk-forward re-weighting for every one of 4 leave-one-out variants was judged excessive scope for a screening tool; the final config still gets full walk-forward validation (§5) |
| India origin signal: both Arabica and Robusta in v1, Kodagu district only | Both species grown in Kodagu (India's largest coffee district, ~30% of national output) — one climate source covers both. Multi-district averaging deferred to a later phase, not a demo blocker |
| ~~India price leg: WB benchmark proxy, not a new scraped source~~ **SUPERSEDED 2026-07-22** | The original timebox (~30-45min) found nothing because it only tried `indiacoffee.org` (dead). Told to keep looking rather than accept a proxy, a second pass found the real `coffeeboard.gov.in` domain within a similar amount of effort — the lesson is the timebox wasn't the limiting factor, trying only one domain was |
| `chirps_india.py` is a new module, not a parameterized `chirps.py` | `chirps.py` backs the validated, live Brazil composite — refactoring tested/working code for marginal reuse benefit wasn't worth the risk. Only the module constants differ (GAUL level-2 vs level-1, species-specific flowering vs Sep-Nov, Kodagu vs Minas Gerais) |
| India climate risk has no `climate_features.py` wrapper | India has one climate input (Kodagu rainfall); Brazil's `climate_risk_score()` exists to weight-combine four inputs. `chirps_india.py`'s own risk score is India's climate risk input directly — a wrapper would be pure ceremony |
| Notebook 09's FAILED gate reported as-is, not reframed or hidden | Calibrated-honesty principle applies to negative results too, on both the proxy-data pass and the real-data pass. On real data the FAIL is even more informative: a `price_position_52w` sanity check confirms the price/mechanics are sound, so the FAIL says something real about the specific climate hypothesis, not an artifact to explain away |
| `coffee_board_india_price.py`/`coffee_board_india_supply.py` built as new sources rather than retrofitting `world_bank_commodity.py` | Genuinely different fetch mechanics (stateful ASP.NET postback chain vs. WB's simple Excel download) and genuinely India-origin data — no shared code to reuse, same "don't force a parameterization" judgment already applied to `chirps_india.py` vs `chirps.py` |
| `FeatureObservation`'s missing `metadata` field handled by encoding into `feature_name` (e.g. `"production_mt:kodagu:arabica"`), not by adding a metadata field to the model | Smaller, more contained fix than changing a shared core model for one source's needs; consistent with how other multi-dimensional data in this repo is already namespaced through `feature_name`/`asset_id` rather than a generic metadata bag |
| Composite confidence for `generate_india_signal()` lowered from 0.4 (v1, "accumulating validation") to 0.3 (v2, real-data FAIL) | A tested-and-failed climate mechanism on real data warrants less confidence than an untested proxy — the confidence number should track what's actually been validated, not just whether *a* backtest ran |
| Declined to bypass SSL certificate verification to recover the lost supply backfill from `coffeeboard.gov.in` | The site's cert had genuinely expired (confirmed via `openssl`), not a client-side misconfiguration — disabling verification trades a data-loss problem for a MITM/data-integrity risk on a source this signal depends on. Recovered via legitimate partial means (locally cached PDFs) instead; full re-backfill waits for the cert to be renewed on their end |
| District-name aliasing (`_REGION_ALIASES`) fixes spelling drift but does not merge structurally different rows | "Chikkamagaluru"/"Chikmagalur" is the same district spelled differently across PDF editions — safe to merge. "Andhra Pradesh" vs "Andhra Pradesh & Orissa" is a genuinely different combined-vs-single-state quantity in older editions — merging would silently conflate two different figures, so it was deliberately left unmerged |
| India pass-through regression uses log-log form, not plain price levels | FX pass-through is multiplicative (a weaker rupee scales the INR value of a fixed USD price) — log coefficients read directly as elasticities, avoiding conflating a multiplicative relationship with an additive one. A literal reading of the review's suggested spec (`price ~ global_price + fx`) would have worked numerically but been the wrong econometric shape |
| Residual re-test needed a new additive-difference helper (`fwd_change_from_month`), not a reuse of `fwd12_from_month`'s percent-change | The pass-through residual is a signed log-quantity that crosses zero — a ratio-based percent change is mathematically incoherent there (blows up near zero, ratio sign doesn't mean what it means for a price level). Added a small parallel helper rather than force-fitting the existing one; `fwd12_from_month`/`fwd_from_month_at_lag` stayed untouched for the raw-price tests |
| India pass-through/FX data stays notebook-local (no new `fx_rates.py` production source) | Mirrors the L2a/L3 precedent and the `FX_USD_INR` registry asset's own metadata ("card-copy framing only, not a composite input") — the global benchmark reuses notebook 07's existing cache and FX reuses notebook 08's fetch pattern verbatim, so there's no new production dependency to justify |

*Update: 2026-07-28 — **tested and confirmed external review's global-pass-through-plus-FX diagnosis of the India climate gate's FAIL.** Review of notebook 09 (both species FAIL, both prior experiments closed negative) offered a structural explanation rather than another climate tweak: India is ~3.5% of world coffee production and exports most of it — a price taker, not a price setter — so the causal arrow should run global-price-and-FX → India price, not local-weather → India price. Built notebook 09 §6-7 to test this directly, reusing existing infrastructure rather than new production sources: the global benchmark is notebook 07's already-cached World Bank Pink Sheet series; USD/INR is `yfinance`'s `INR=X` via notebook 08's already-validated fetch pattern (that notebook's own ticker list doesn't include INR, but the call needed no modification). Model: `log(india_price) ~ log(global_price) + log(usd_inr)`, log-log OLS (not plain levels — FX pass-through is multiplicative, so log coefficients read as elasticities). **Result: R²=0.887 (Robusta) / 0.962 (Arabica)**, both FX coefficients positive as expected and highly significant (p<0.001) — global pass-through + FX explains 89-96% of India's domestic price, not a partial explanation. Re-ran the same flowering-deficit gate against the pass-through **residual** (the ~4-11% pass-through doesn't explain) at the same ≥+0.30 bar: both species still FAIL — Arabica r=+0.033 (zero); **Robusta r=−0.587 (p=0.045, n=12), more significant AND more wrong-signed than the raw-price test**, stable across a 6-24m lag sweep (−0.502 to −0.587 in the 12-24m band). Read plainly, a wetter Robusta flowering season associates with a *higher* pass-through-adjusted price a year-plus later — reported as a genuinely surprising result, not explained away. One implementation correction made while building this: reusing §3's percent-change helper (`fwd12_from_month`) directly on the signed, zero-crossing residual would have been mathematically incoherent (division near zero, ratio sign not meaningful for a signed quantity) — added a small parallel additive-difference helper (`fwd_change_from_month`) instead of forcing the ratio-based one to fit. Notebook renumbered (composite demo §5→§8, Summary §6→§9) to fit the new §6-7 sections in narrative order; re-executed cleanly via nbconvert, `ruff check` passes, `make verify` green. Documented in `docs/india_origin_signal_plan_v2_full_build.md` §14. This resolves rather than deepens the "what's actually driving India price" question left open after the two prior experiments — the product-narrative implication (a validated India signal looks like global-price-plus-FX, not local climate) now feeds the still-pending card-copy review. Two items remain deliberately out of scope, each meaningfully larger than this pass and deferred to their own dedicated work: discrete regulatory/policy event dummies (EUDR, India-EFTA) and harvest-window (not flowering-window) rainfall as a different climate pathway. Next: card-copy review incorporating the pass-through framing; full 62-report supply re-backfill once Coffee Board's certificate is renewed (external, still blocked); either of the two deferred items if prioritized.*

*Prior: 2026-07-22 (later same day) — **closed out both flagged India follow-up experiments, found a data-quality bug, and handled a real external incident.** Continuing directly from the real-data rebuild below: implemented the two "not yet tried" items from that pass. (1) Extended `chirps_india.py` with a `district` parameter (Kodagu/Chikmagalur/Hassan) and built a production-weighted multi-district climate signal — does not rescue the gate (Robusta r=−0.357→−0.285, still FAIL; Arabica r=−0.038→−0.016, still FAIL), weak evidence against "wrong district" as the cause. (2) Ran leave-one-out and 5,000-resample bootstrap robustness checks on the Arabica 24m r=+0.179 hint — sign flips under leave-one-out ([−0.065, +0.395]) and the bootstrap 90% CI ([−0.356, +0.622]) spans both zero and the gate; concluded this was noise, not a real weak effect, resolving the ambiguity the prior update left open. While building the district weights, found and fixed a real data-quality bug in `coffee_board_india_supply.py`: district names spell differently across PDF editions ("Chikkamagaluru"/"Chikmagalur", "Wyanad"/"Wayanad", "Orissa"/"Odisha" — the last a genuine 2011 state rename), splitting one district's series in two; fixed with a `_REGION_ALIASES` map, deliberately not merging the structurally-different "Andhra Pradesh & Orissa" combined row. Attempting to re-fetch the full supply backfill (to apply the alias fix across all 62 reports) hit a real external event: `coffeeboard.gov.in`'s TLS certificate expired 2026-07-22 (confirmed via direct `openssl` inspection) and the site went fully unreachable — compounded by a caching-script mistake that overwrote the good 1,289-observation CSV with an empty result without checking success first, destroying it. Declined to bypass SSL certificate verification to route around this (MITM/data-integrity risk not worth trading for a data-recovery shortcut); recovered a reduced 168-observation/4-edition snapshot from locally-cached PDFs instead — sufficient for the district-weight experiment, not for redoing the full YoY supply test at its original power. Notebook 09 rebuilt with both experiments as new sections (§4c, §4d) and a comprehensively rewritten summary; re-executed cleanly via nbconvert, `ruff check` passes. Documented throughout in `docs/india_origin_signal_plan_v2_full_build.md` §13 rather than smoothed over. Next: full 62-report re-backfill once Coffee Board renews their certificate (external, blocked); explore a fundamentally different climate construction (monsoon-season rainfall vs. blossom-window SPI-3) or accept the mechanism as untestable at India's current price-history length; card-copy review before any roaster demo.*

*Prior: 2026-07-22 — **corrected the India origin signal's price-source conclusion and rebuilt on real data.** Yesterday's pass concluded no India-origin price source existed and fell back to a WB benchmark proxy — told explicitly not to settle for that and not to self-impose artificial timelines, a second, unhurried pass found the real thing: Coffee Board of India's actual live domain is `coffeeboard.gov.in` (`indiacoffee.org`, tried yesterday, is simply dead). Built `coffee_board_india_price.py` (a stateful ASP.NET scraper for the Daily Market Report archive, reverse-engineered by replaying the flow with `requests.Session()` and cross-checking against a live browser session) and `coffee_board_india_supply.py` (semiannual production-estimate PDF circular, vintage-dated like `usda_coffee_wmt.py`). Discovered along the way that the "daily" price figure only actually updates ~weekly (confirmed by observing repeated values across consecutive archived days, not assumed) and that `httpx.Client` needs `follow_redirects=True` explicitly (a third live-only bug, unrelated to India in cause). Ran a full paced historical backfill (9,217 price observations 2012-2026, dense from 2014; 1,289 supply observations 2010-2024) and corrected `chirps_india.py` to species-specific flowering windows (Robusta Feb-Mar, Arabica Apr-May — the original used one shared window, verified wrong for Arabica against real agronomy sources). Rebuilt notebook 09 entirely on this real data, adding a lag sweep and a `price_position_52w` sanity check before concluding — **the gate still FAILS for both species** (Robusta r=−0.357 at 12m, wrong-signed at every horizon 3-24m tested; Arabica r=−0.038 at 12m, turns +0.179 at 24m but still short), and this time it's a diagnosed result, not just a proxy-limitation excuse: the sanity check confirms the price data and mechanics are sound, so the FAIL says something real about the specific climate hypothesis under test. Documented honestly rather than reframed — see `docs/india_origin_signal_plan_v2_full_build.md` §12 for the full writeup. `make verify` run before considering this workstream done. Next: try a production-weighted multi-district climate signal; retest the Arabica 24m hint once more price history accumulates; full card-copy review before any roaster demo.*

*Prior: 2026-07-21 — **built the India origin signal** (Arabica + Robusta, Kodagu) end to end in a single sprint on a new branch (`feat/india-origin-signal`), reusing the validated global-Arabica infrastructure throughout: `price_position_52w` unmodified, the SPI-3 gamma-fit methodology from notebook 05, and the composite formula shape via a new `generate_india_signal()`. Corrected the outdated `docs/india_origin_signal_plan_v2_full_build.md` against current repo state first. Task 0 (timeboxed price/climate audit) found no scrapeable India-origin price history, triggering the pre-agreed WB-benchmark-proxy fallback — and surfaced a real, unrelated production bug along the way (World Bank silently changed the Pink Sheet Excel's header-row structure on 2026-07-02, breaking `world_bank_commodity.py`'s live fetch; fixed and covered by new tests). Implemented `chirps_india.py` (new module, GAUL level-2 Kodagu, Feb-Mar flowering) and found a second live-only GEE bug (`Dictionary.get(key, None)` still raises — needed a real sentinel) while extending the fetch range to `date.today()`. Filled in the previously-stubbed `build_recommendation()` and ran notebook 09: **gate FAILED for both species** (r=−0.214 Arabica, r=+0.132 Robusta, both vs +0.30 bar) — an honest, expected result of the proxy-price limitation, not a bug, and documented as such rather than reframed. The composite is real and runs live for demo purposes (confidence=0.4) but is explicitly not validated for roaster-facing promotion yet. `make verify` run before considering this workstream done. Next: re-audit if a genuine India-origin price source becomes available; full card-copy calibrated-honesty review before any roaster demo.*

*Prior: 2026-07-20 — **rebuilt notebook 06 around a continuous signal, after the product owner flagged the first version's sparse buy-signal result as a real product problem.** Re-read `docs/GreenSignal_ICP.md`/`coffee_intelligence_mvp.md`/`GreenSignal_Phase0_Report.md`/`GreenSignal_Math_Reference.md` and confirmed GreenSignal is designed as an always-on 3-state signal (Phase 0 target 22% BUY/31% CAUTION/47% NEUTRAL for L1 alone; real data confirms 35–41% BUY, not sparse). Found and fixed the root cause: the walk-forward's annual-refit normalization baseline (vs. a rolling 24-month one) was responsible for most of the sparsity (4.9% → 20.0% BUY rate). Switched the primary gate to the continuous `cost_improvement_backtest` methodology (matching notebook 01's approach for L1 alone) instead of the discrete prescience test — **PASSES: walk-forward +5.86%, beating L1-alone's own +3.71%**. Confirmed the spike-avoidance story directly (sustained BUY signal before the 2024 rally). Two anomalies surfaced and reported honestly rather than hidden: the secondary prescience check shows BUY underperforming CAUTION/NEUTRAL in this sample, and the ablation reverses L5's sign vs. the original notebook. Also documented that 3 collaborators are independently exploring different composite formulas right now (`CLAUDE.md`, this file) rather than implying a single converged design — this rebuild specifically targets the multiplicative formula. Branched off current `main` (both prior PRs #6/#7 already merged) as `feat/notebook-06-continuous-signal-rebuild`. `make verify` green (134 tests). Next: reconcile the two open anomalies, rebuild `stu_stress` off the true-vintage series, then production wiring.*

*Prior: 2026-07-13 (session continued) — **built the full composite backtest (notebook 06)** and fixed a real L5 documentation-drift bug found while planning it. See the "Composite Backtest" entries above (line 38-40) for the notebook's results. Also added missing `Save Reference CSV` cells to notebooks 01 and 03 (only two of the five still missing this, discovered while assembling notebook 06's inputs) and re-executed both. Branched off the still-open L2a PR (`feat/notebook-04-vintage-rebuild`, #6) rather than off `main`, since notebook 06 needs nb04's `stu_wmt_vintage.csv` output — this PR is stacked on #6. `make verify` green (134 tests). Next: rebuild `stu_stress` off the true-vintage series and re-run the L2a ablation, then production wiring (`build_recommendation`, `climate_risk_score` weights, `risk_scorer.py`).*

*Prior: 2026-07-13 — **rebuilt L2a (USDA stocks-to-use) around a vintage correction, then built the true vintage source.** Phase 1: after review feedback flagged the original notebook's use of the PSD bulk file (always the latest-*revised* vintage) as a look-ahead bias, shifted the monthly S/U series forward 12 months to simulate the real publication lag (raw r(S/U, price level)=−0.40 weakens to r=−0.26 once corrected). Redefined the single YoY-change gate into two tests on the vintage-lagged series, both **PASS**: Gate 1 r=−0.312 (p=2.0e-5, n=180), Gate 2 r=−0.259 (p=4.6e-4, n=180). Added a 10yr rolling z-score, YoY delta, months-of-consumption features, and a z-score-based non-linear stress score. Phase 2 (same session, user asked to pursue the "gold standard" fix): the feedback's suggested source, WASDE, turned out not to cover coffee at all (verified via web search) — implemented `domains/coffee/sources/usda_coffee_wmt.py` instead, parsing USDA's actual semiannual "Coffee: World Markets and Trade" circular for a genuinely time-stamped series (24 tests, reviewed via `data-source-reviewer` — **APPROVED**). Wired into notebook 04 §11: the true vintage series gives a **stronger** Gate 1 (r=−0.488, p=6.2e-3, n=30) than the approximation, with Gate 2 passing the threshold but not reaching significance at this small semiannual sample (r=−0.261, p=0.17). Kept both series in the notebook — true vintage as primary evidence, the shift approximation as a documented fallback for pre-June-2010 and monthly-cadence gaps. Declined to promote the new features/source into production `supply_features.py`/`usda_psd.py` yet (per the L3/SPI precedent, wait for composite wiring in notebook 06). `make verify` green (134 tests). Next: composite backtest (06).*

*Prior: 2026-07-02 — **rebuilt L3 (CHIRPS) around SPI** per a commodity-desk review. Replaced the raw-mm anomaly with a gamma-fit Standardized Precipitation Index; primary feature is now the flowering **SPI-3 (Sep–Nov) deficit** on the annual crop-year frame. Redefined the gate (annual r ≥ +0.30, confirming signal) — **PASSES at r=+0.483 (p=0.069, n=15)**, up from the old raw-mm +0.398. Added expanding-window (no look-ahead, r=+0.494) and partial-correlation-vs-stocks (r=+0.48) robustness checks, and a tercile event study (dry vs wet flowering third → +33.6% vs +9.5% fwd-12m). Deferred Vietnam-monsoon / new-crop-contract / forward-cover / options items to later phases (notebook 05 §10). `make verify` green (110 tests). Next: promote the SPI feature to `climate_features.py` and run the composite (06).*

*Prior: 2026-06-08 — **corrected the L2b ENSO thesis.** The original "La Niña droughts Brazil/Vietnam, 24m lag" had the sign AND lag backwards. Verified the country matrix (`docs/enso_coffee_country_matrix.html`) against peer-reviewed sources: El Niño is the dominant supply-risk phase (droughts Vietnam/Indonesia robusta; La Niña is beneficial there & in Brazil, only hurts Colombia). Flipped `enso_risk_score` in `noaa_enso.py` and rebuilt notebook 02 — redefined gate (r ≥ +0.20 vs fwd YoY at ~14m lead) now **PASSES**: r=+0.288 @15m (KC) / +0.327 @15m (WB 2000–24); event study El Niño→+36.5% vs La Niña −1.7%. 26 ENSO tests updated & green. Next: composite backtest (notebook 06) — L1 strong, L2a strong-on-level, L2b now a validated ~14m lead, L3/L5 weak amplifiers; then redefine L2a gate, decide L5 fate.*

*Prior update: 2026-05-28 — implemented `chirps.py` (L3) via GEE + notebook 05 (gate narrowly FAILS r=+0.10 @ 14m, but right sign/lag/mechanism, annual flowering r=+0.40). All 5 sources implemented and backtested on real data. 110 tests pass, `make verify` green.*
